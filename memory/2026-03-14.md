# 2026-03-14

- Investigated isolated cron failures around 2026-03-14 02:00 UTC. Multiple jobs returned `SEND_FAILED: exec unavailable in this session` even though global config still had `tools.profile: full` and no agent-specific tool deny was present.
- Verified this was likely transient/runtime-specific rather than permanent config drift: a fresh throwaway isolated cron repro (`temp-cron-exec-repro`) successfully executed `/bin/echo CRON_EXEC_OK` afterward under the same agent/runtime.
- Strong working theory: OpenClaw 2026.3.7 hit a transient isolated-cron tool-registration/bootstrap issue after a gateway restart around 00:53 UTC; recommended fix path is upgrade to OpenClaw 2026.3.12, add an isolated-cron exec self-test after restarts, and rerun affected jobs once runtime is healthy.
