# Canon Workspace — Personalized Daily Tracker MVP

For this project, the canonical working repository is on the Mac node at:

`~/Downloads/06 dev/life-tracker/app`

Project root (design + docs + app):

`~/Downloads/06 dev/life-tracker`

Notes:
- `Home.png` is the current homescreen design reference.
- `life.pen` is the Pencil source artifact currently available.
- Frontend implementation is in `app/`.

All implementation reviews should reference this path unless Boss explicitly changes it.
